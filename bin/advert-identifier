#!/usr/bin/env python3
"""Entry point wrapper for advert-identifier (OCR detection)."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / "src"))
from ad_break_identifier.detect import main

sys.exit(main())
